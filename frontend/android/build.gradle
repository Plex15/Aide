buildscript {
    ext {
        buildToolsVersion = "35.0.0"  // Use the correct build tools for SDK 34
        minSdkVersion = 24
        compileSdkVersion = 34      // Use the latest stable SDK
        targetSdkVersion = 34       // Match the compile SDK
        ndkVersion = "27.0.11902837"   
        kotlinVersion = "1.9.23"      // A recent stable Kotlin version
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"
